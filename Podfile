# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS Sample Architecture' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS Sample Architecture
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
  pod 'Kingfisher', '~> 5.0'

  target 'iOS Sample ArchitectureTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
